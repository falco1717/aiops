# The image is built on Linux; keep everything LF in the repo regardless of the
# checkout platform so Docker never sees CRLF in a Dockerfile or entrypoint.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
